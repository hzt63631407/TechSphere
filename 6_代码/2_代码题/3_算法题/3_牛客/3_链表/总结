

1.向前移动时
如果需要知道pre，那么向前移动时()
pre = cur
cur = cur.next

如果不需要知道pre（删除重复节点）
cur = cur.next

2.用while时
如果用到cur.next         那么条件是cur
如果用到cur.next.next    那么条件是cur.next

3.删除节点
pre.next = cur.next 删除某个节点
cur.next = cur.next.next 删除重复节点

class ListNode:
    def __init__(self,x):
        self.val = x
        self.next = None

l = ListNode('4')
r = ListNode('5')
s = ListNode('6')
l.next = r
r.next = s
